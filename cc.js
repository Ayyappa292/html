const { map } = require("underscore");

var sa=[{Name: "Apple",Colour: "Red",Taste: "Sweet"}]
var ss=sa.find(element=>element=Colour)
console.log(ss);
sa.forEach(element => {
    
});
array.forEach(element => {
    
});
map